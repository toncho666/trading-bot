"""
Конфигурация модуля.
Замените значения YOUR_… на свои реальные/демо-ключи.
"""
import os
# ----------------------------------------------------------------------
#  API‑ключи. Доступ к демо‑счету получаете в разделе «Testnet» на Bybit.
# ----------------------------------------------------------------------
API_KEY = os.getenv("BYBIT_API_KEY")
API_SECRET = os.getenv("BYBIT_API_SECRET")

# ----------------------------------------------------------------------
#  URL‑endpoint. По умолчанию в библиотеке уже выставлен правильный.
#  Если хотите переключаться между реальной и тестовой площадкой
#  без изменения кода, просто укажите нужный URL здесь.
# ----------------------------------------------------------------------
# REAL:   "https://api.bybit.com"
# TESTNET:"https://api-testnet.bybit.com"
# BASE_URL = "https://api.bybit.com"          # реальный счёт
BASE_URL = "https://api-testnet.bybit.com"  # демо‑счёт (раскоментировать при необходимости)

# ----------------------------------------------------------------------
#  Параметры по умолчанию (можно переопределять при вызове)
# ----------------------------------------------------------------------
DEFAULT_LEVERAGE = 10           # плечо, если еще не выставлено
# DEFAULT_ORDER_TYPE = "Limit"    # "Market" тоже поддерживается
DEFAULT_ORDER_TYPE = "Market"    # "Limit" тоже поддерживается
DEFAULT_TIME_IN_FORCE = "GoodTillCancel"
